allOf:
  - $ref: "./NucleusKit.yaml"
  - title: KitFull
    type: object
    properties:
      commands:
        type: array
        description: The commands that are executed for the player when receiving this kit.
        items:
          type: string
      stacks:
        type: array
        description: The items that the player receives when aquiring this kit.
        items:
          $ref: "../ItemStack.yaml"
