description: Block operation
schema:
  title: BlockOperationResponse
  type: object
  properties:
    ok:
      $ref: "#/definitions/ok"
    operation:
      $ref: "#/definitions/BlockOperationFull"
